{
  "devDependencies": {
    "serve": "^11.2.0"
  },
  "main": "index.html",
  "name": "swiper-navigation",
  "scripts": {
    "build": "mkdir -p public && cp -r *.html *.css *.js *.json public/ 2>/dev/null || true",
    "start": "serve public"
  },
  "tags": [
    "swiper"
  ]
}